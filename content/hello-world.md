---
title: Hello, World
date: 2026-05-20
---

This is the first post. Front matter is optional — if you omit `title` it's
derived from the filename, and if you omit `date` it falls back to the file's
modification time.

## A subheading

Markdown works the way you'd expect: **bold**, *italic*, [links](https://example.com),
and lists.

- fenced code blocks
- tables
- footnotes[^1]

```python
def hello(name: str) -> str:
    return f"hi, {name}"
```

> A blockquote, for good measure.

[^1]: Like this one.
